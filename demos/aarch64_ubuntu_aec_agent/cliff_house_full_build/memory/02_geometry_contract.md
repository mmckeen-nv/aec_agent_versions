memory_summary: Validate every FreeCAD construction unit and preserve metadata through Blender handoff.
memory_class: procedural_tool_call

FreeCAD is authoritative for architectural geometry; Blender is visualization authority. After each bounded construction unit recompute and report object counts, semantic labels, groups, shape validity, volumes where applicable, and global bounds. Preserve units, axes, identifiers, material roles, and placements. At Phase 07 export an audited deterministic manifest and require per-object X/Y/Z bounding-box parity plus plan and axonometric review in Blender; count equality alone is insufficient. ComfyUI images are never geometry evidence.
