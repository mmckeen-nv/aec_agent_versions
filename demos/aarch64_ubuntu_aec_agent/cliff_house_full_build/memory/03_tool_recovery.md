memory_summary: Use registered tools and stop cleanly when a required bridge is unavailable.
memory_class: procedural_tool_call

Use only MCP tools currently registered by Hermes. On failure capture the exact error, refresh once, run a read-only inventory, then perform a harmless create/query/delete transaction before resuming. Never guess HTTP endpoints or substitute UI automation. If FreeCAD, Blender, or ComfyUI remains unavailable, stop at the applicable gate and state the exact operator action. Never emit a phase receipt without the playbook's evidence.
