# Cleanup and rollback

Close Rhino, Blender, and MCP bridges. Delete only generated `work/` or `outputs/` directories you
explicitly no longer need. Remove the `cliff-house-full-build-windows` project through Hermes'
normal project UI if desired. Do not delete the Hermes profile, credentials, or application
installation because they are user-owned and may serve other projects.
