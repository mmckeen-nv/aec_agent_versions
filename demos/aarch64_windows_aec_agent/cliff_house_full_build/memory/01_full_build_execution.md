memory_summary: Start or resume the full build from its phase contract and construct through Rhino MCP.
memory_class: procedural_tool_call

For “build the cliff house,” read the skill index, Windows workspace rules, demo rules, phase state, project brief, and active phase prompt in that order. Start Phase 00 or resume only from a valid receipt. Use the source-curve `base_model.3dm`; never use HERO, MASTER, or completed quick-demo geometry. Inspect through registered Rhino MCP tools, then use bounded `run_python` operations for each approved construction unit. After each operation return object counts, named layers, bounds, validity checks, and the phase receipt required by the playbook.
