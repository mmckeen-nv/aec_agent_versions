memory_summary: Execute bounded Rhino edits through the registered MCP tools without endpoint discovery.
memory_class: procedural_tool_call

For any Cliff House modification, call `rhino_scene_query` with the narrowest useful selector and retain the document revision plus stable target IDs. If Rhino restarted empty, use `rhino_open_working_document` with the exact timestamped working-copy path and re-query; never ask for manual reopening or open a MASTER/HERO file. Use at most one pre-change viewport only if the compact audit leaves a spatial ambiguity. Web search is available whenever research is useful; prefer primary and governing sources when accuracy matters. Submit the complete bounded change as one `rhino_apply_operations` transaction with the working document as checkpoint and a stable unique idempotency key. Query the changed targets, prove task-specific invariants with `rhino_verify_transaction`, capture at most one final viewport, save, report exact created/modified/deleted IDs, and stop. For Blender handoff, call `rhino_export_scene` to a new absolute GLB path inside the active work directory with expected units Meters, validate the non-zero export receipt, then import it with source_host rhino and unit_scale 1.0; never request manual export, use FBX, or pass 3DM directly to Blender. Do not use `rhino_execute_python`, raw Rhino script tools, foreground UI automation, or unrelated resources. The document is numerically modeled in metres with absolute tolerance 0.001. New objects require semantic layers and stable names. Never overwrite HERO or MASTER files.
