memory_summary: Execute bounded Rhino edits through the registered MCP tools without endpoint discovery.
memory_class: procedural_tool_call

For Cliff House modification, perform one request-scoped `run_csharp` audit returning only document path, units/tolerance, total object count, and relevant object IDs, names, layers, and bounds. Never call unrestricted `list_objects`. Use at most one pre-change viewport only if the compact audit leaves a spatial ambiguity. Web search is available whenever research is useful; prefer primary and governing sources when accuracy matters. Perform the whole requested change in one `run_python` call. Then perform one compact `run_csharp` validation, capture one viewport, save, report evidence, and stop. Do not query DML tools, load skills, search sessions/files, inspect unrelated resources, or use foreground UI automation. The document is numerically modelled in metres with absolute tolerance 0.001. New objects require explicit semantic layers and stable names. Never overwrite HERO or MASTER files.
